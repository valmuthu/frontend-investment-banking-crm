# Frontend .env.local file (create this in your frontend root directory)
REACT_APP_API_URL=http://localhost:5000
REACT_APP_ENV=development

# Alternative for Vite (if using Vite instead of Create React App)
VITE_API_URL=http://localhost:5000
VITE_ENV=development
